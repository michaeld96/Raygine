{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Game Engine",
            "command": "make",
            "args": [
                // You can specify additional make arguments here if needed
            ],
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}